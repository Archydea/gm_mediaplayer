name: Развертывание static контента на страницы Github'а

on:  
  push:  
    branches: ["main"]  
    paths:  
      - 'public/**'  
      - '.github/workflows/static.yml'  
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Настройка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # cache: 'npm'

      - name: Install dependencies
        run: |
          echo "::group::Установка инструментов для минификации"
          npm install -g html-minifier-terser clean-css-cli terser
          echo "::endgroup::"

      - name: Verify public directory
        run: |
          echo "::group::Verifying public directory contents"
          echo "Files in public directory:"
          find public -type f | head -20
          echo "Total files: $(find public -type f | wc -l)"
          echo "::endgroup::"

      - name: Минификация HTML файлов
        run: |
          echo "::group::Minifying HTML files in public/"
          find public -name "*.html" -type f | while read file; do
            echo "Processing: $file"
            original_size=$(stat -c%s "$file")
            html-minifier-terser \
              --collapse-whitespace \
              --remove-comments \
              --remove-optional-tags \
              --remove-redundant-attributes \
              --remove-script-type-attributes \
              --remove-tag-whitespace \
              --use-short-doctype \
              --minify-css true \
              --minify-js true \
              --output "$file" \
              "$file"
            new_size=$(stat -c%s "$file")
            savings=$((original_size - new_size))
            echo "✓ $file: ${original_size}B → ${new_size}B (saved ${savings}B)"
          done
          echo "::endgroup::"

      - name: Минификация CSS файлов
        run: |
          echo "::group::Минифицируем CSS файлы в public/"
          find public -name "*.css" -type f | while read file; do
            echo "Processing: $file"
            original_size=$(stat -c%s "$file")
            cleancss --output "$file" "$file"
            new_size=$(stat -c%s "$file")
            savings=$((original_size - new_size))
            echo "✓ $file: ${original_size}B → ${new_size}B (saved ${savings}B)"
          done
          echo "::endgroup::"

      - name: Minify JavaScript files
        run: |
          echo "::group::Минифицируем JavaScript файлы в public/"
          find public -name "*.js" -type f | while read file; do
            echo "Processing: $file"
            original_size=$(stat -c%s "$file")
            terser "$file" --compress --mangle --output "$file"
            new_size=$(stat -c%s "$file")
            savings=$((original_size - new_size))
            echo "✓ $file: ${original_size}B → ${new_size}B (saved ${savings}B)"
          done
          echo "::endgroup::"

      - name: Генерация репорта о сборке
        run: |
          echo "::group::Сводка"
          echo "## Build Report" >> $GITHUB_STEP_SUMMARY
          echo "| Тип файла | Кол-во | Общий размер |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|--------------|" >> $GITHUB_STEP_SUMMARY

          html_count=$(find public -name "*.html" | wc -l)
          html_size=$(find public -name "*.html" -exec stat -c%s {} + | awk '{sum+=$1} END {print sum}')
          echo "| HTML | $html_count | ${html_size:-0} байтов |" >> $GITHUB_STEP_SUMMARY

          css_count=$(find public -name "*.css" | wc -l)
          css_size=$(find public -name "*.css" -exec stat -c%s {} + | awk '{sum+=$1} END {print sum}')
          echo "| CSS | $css_count | ${css_size:-0} байтов |" >> $GITHUB_STEP_SUMMARY

          js_count=$(find public -name "*.js" | wc -l)
          js_size=$(find public -name "*.js" -exec stat -c%s {} + | awk '{sum+=$1} END {print sum}')
          echo "| JavaScript | $js_count | ${js_size:-0} байтов |" >> $GITHUB_STEP_SUMMARY

          total_files=$(find public -type f | wc -l)
          total_size=$(find public -type f -exec stat -c%s {} + | awk '{sum+=$1} END {print sum}')
          echo "| **Всего** | **$total_files** | **${total_size:-0} байтов** |" >> $GITHUB_STEP_SUMMARY

          echo "Билд успешно завершился с $total_files файлами общего размера в $total_size байтов"
          echo "::endgroup::"

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'public/'

      - name: Отправка на GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: УСПЕХ
        if: success()
        run: |
          echo "::notice::Успех! Сайт доступен на ${{ steps.deployment.outputs.page_url }}"

      - name: СБОЙ
        if: failure()
        run: |
          echo "::error::Столкнулись с ошибкой во время развертывания, детали в логах."
